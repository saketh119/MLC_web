Place your chosen logo image(s) in this `public/` folder and name the primary logo `logo-primary.png`.

Suggested filenames:
- `logo-primary.png`  — used by `src/components/Navbar.tsx` (40x40px recommended)
- `logo-primary@2x.png` — optional high-DPI variant

You provided logo images as attachments; add the one you prefer here and name it `logo-primary.png`. The Navbar already points to `/logo-primary.png`.

If you prefer an SVG, name it `logo-primary.svg` and update `src/components/Navbar.tsx` accordingly.
